@model IEnumerable<Projet_Navette.Models.LigneAbonnement>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">

    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Offre)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        var nomcomplet = item.Client.nom + " " + item.Client.prenom;
    <tr>
        <td>
        @nomcomplet        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Offre.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.id_LigneAbonnement }) |
            @Html.ActionLink("Details", "Details", new { id = item.id_LigneAbonnement }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.id_LigneAbonnement })
        </td>
    </tr>
}

</table>
